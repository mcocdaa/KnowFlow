import { Typography } from 'antd';

const LibraryPage = () => (
  <div>
    <Typography.Title level={3} style={{ marginTop: 0 }}>
      知识库
    </Typography.Title>
    <Typography.Paragraph type="secondary">列表与搜索功能建设中。</Typography.Paragraph>
  </div>
);

export default LibraryPage;

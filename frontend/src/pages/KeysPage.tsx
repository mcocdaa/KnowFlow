import { Typography } from 'antd';

const KeysPage = () => (
  <div>
    <Typography.Title level={3} style={{ marginTop: 0 }}>
      Key 管理
    </Typography.Title>
    <Typography.Paragraph type="secondary">Key 管理功能建设中。</Typography.Paragraph>
  </div>
);

export default KeysPage;

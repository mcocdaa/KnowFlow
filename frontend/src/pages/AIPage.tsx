import { Typography } from 'antd';

const AIPage = () => (
  <div>
    <Typography.Title level={3} style={{ marginTop: 0 }}>
      AI 助手
    </Typography.Title>
    <Typography.Paragraph type="secondary">AI 助手功能建设中。</Typography.Paragraph>
  </div>
);

export default AIPage;

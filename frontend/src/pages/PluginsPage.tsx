import { Typography } from 'antd';

const PluginsPage = () => (
  <div>
    <Typography.Title level={3} style={{ marginTop: 0 }}>
      插件
    </Typography.Title>
    <Typography.Paragraph type="secondary">插件列表功能建设中。</Typography.Paragraph>
  </div>
);

export default PluginsPage;
